{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "backend for fullstack mooc exercise",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.s",
    "test": "echo \\Error: no test specified\\ && exit 1",
    "build:ui": "rm -rf build && cd ../fullstackopen/part1/puhelinluettelo/ && npm run build --prod && cp -r build ../../../phonebook-backend",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/naukatti/phonebook-backend.git"
  },
  "author": "Noora Peltokangas",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/naukatti/phonebook-backend/issues"
  },
  "homepage": "https://github.com/naukatti/phonebook-backend#readme",
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  }
}
